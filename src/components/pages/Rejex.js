import React from 'react'

 export const validPassword = new RegExp(
    '^(?=.*?[A-Za-z])(?=.*?[0-9]).{6,}$'
    );


const Rejex = () => {
  return (
    <>
    h1
    </>
  )
}

export default Rejex